# Ponder Hero Marquee Assets

Downloaded from the existing <https://ponder.ing> Hero section marquee.

These are the deduplicated source images used by the `home-index-college-img` marquee logo strip.

Source URLs are recorded in `sources.json`.
